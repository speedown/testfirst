spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/many2many
    username: postgres
    password: Gold7869226#
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

